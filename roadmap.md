# データベース学習ロードマップ

## 全体像

「SQLで操作できる → 正規化とER図で論理設計できる → 実務の失敗パターンを知る」の順に進める。
各フェーズで完璧を目指さず、一周してから必要に応じて戻る。

| フェーズ | テーマ | 主な教材 | 状態 |
|---|---|---|---|
| Phase 1 | SQLの基礎 | 『SQL 第2版 ゼロからはじめるデータベース操作』 | **進め方決定・着手** |
| Phase 2 | 論理設計（正規化・ER図） | 『達人に学ぶDB設計徹底指南書 第2版』 | 教材候補のみ |
| Phase 3 | 要件定義〜運用までの全体像 | 『やさしいデータベース設計 要件定義から運用までの勘どころ』 | 教材候補のみ（任意） |
| Phase 4 | 実践・アンチパターン | 『SQLアンチパターン 第2版』 | 教材候補のみ（辞書的に使う） |
| 並行 | 設計の実践 | Card Pulse のテーブル設計 | 未着手 |

---

## Phase 1：SQLの基礎

### 目的

- SQLでデータベースを操作できるようになる（Phase 2以降の設計本を読むための土台）
- PostgreSQLの環境をDockerで扱えるようになる

### 教材

- **『SQL 第2版 ゼロからはじめるデータベース操作』**（手持ち）
  - 本書は主にPostgreSQLを使って解説している

### 環境：DockerでPostgreSQLを動かす

PCに直接インストールせず、PostgreSQLの公式イメージをコンテナで動かす。

**Dockerを使う理由**

- 本とほぼ同じ環境（PostgreSQL）をすぐ用意できる
- 練習で壊しても、コンテナを作り直せば数分で元に戻せる
- Docker Composeで定義すれば、インフラ学習（オーケストレーションの入口）にもつながる

**環境構築時の注意点**

- **本の環境構築の章とのズレ**：本はPCへの直接インストールを前提にしている。「PostgreSQLが動いて、psqlで接続できる状態を作る」という目的だけ押さえ、手順は読み飛ばしてよい。2章以降のSQLはそのまま使える
- **データの永続化**：ボリュームを設定しないと、コンテナ削除時にテーブルも消える。壊してもよい練習用なら不要、残したいなら設定する
- **バージョン固定**：イメージのタグは `latest` ではなく、`16` などに固定する
- **ポート衝突**：ホストから接続する場合、ローカルに別のPostgreSQLがあると5432番ポートが衝突する
- **日本語データ**：文字化けしたらエンコーディング（UTF-8）を確認する

### 接続方法：コンテナの中でpsqlを使う

公式イメージにはpsqlが最初から入っているため、追加インストールなしで接続できる。

- **psqlを直接起動する**：`docker exec -it コンテナ名 psql -U ユーザー名`
  → SQLの練習はこれで十分
- **シェルに入ってからpsqlを起動する**：`docker exec -it コンテナ名 bash` → その中で `psql`
  → コンテナ内のファイルを確認したいときに使う
- ホストからポート経由で接続する方法（GUIクライアントなど）は、表形式で結果を見たくなったら追加すればよい

### 押さえておく概念

#### PostgreSQL・SQL・psqlの関係

| 名前 | 役割 |
|---|---|
| PostgreSQL（サーバー） | データを保存し、SQLを受け取って処理する本体 |
| SQL | サーバーに処理を頼むための言語（`SELECT`、`CREATE TABLE` など） |
| psql | 人間がSQLを打ち込んでサーバーに送るための窓口ツール |

#### 「今どこで入力しているか」はプロンプトで判断する

| 場所 | プロンプトの例 | 入力するもの |
|---|---|---|
| ターミナル（シェル） | `$` | `docker`、`psql` などプログラムを起動するコマンド |
| psqlの中 | `postgres=#` | SQL文、メタコマンド |

- `psql` は**最初に1回だけ起動する**。SQL文ごとに `psql` を付ける必要はない
- 例外として `psql -c "SQL文"` でターミナルから1回だけ実行する方法もあるが、学習中は使わなくてよい

#### SQL文とメタコマンドの違い

- **SQL文**：psqlがそのままサーバーに送る。最後に `;` を付けるまで実行されない
  - `;` を忘れるとプロンプトが `=#` → `-#` に変わり、続きの入力待ちになる
- **メタコマンド**：`\` で始まる、**psqlというツール自体の機能**（psqlのショートカット機能）
  - psqlでしか使えない（GUIクライアントやアプリのコードでは動かない）
  - 本で学ぶのはSQLのほうで、メタコマンドは作業確認の補助道具

**よく使うメタコマンド**

| コマンド | 内容 |
|---|---|
| `\l` | データベース一覧 |
| `\c データベース名` | データベースを切り替える |
| `\dt` | テーブル一覧 |
| `\d テーブル名` | テーブルの構造を表示 |
| `\?` | ヘルプ |
| `\q` | psqlを終了してターミナルに戻る |

### 進め方

1. PostgreSQLコンテナを起動する（Docker Composeで定義するのがおすすめ）
2. コンテナ内でpsqlを起動して接続する
3. 本の最初の `CREATE DATABASE` を実行する
4. `\c データベース名` で作ったデータベースに切り替えてから本を進める
5. テーブルを作ったら `\d` で構造を確認し、本の内容と照らし合わせる
6. 詰まったり環境が壊れたりしたら、コンテナを作り直して最初からやり直す
7. 作業が終わったら `\q` でpsqlを終了する

### 学習のコツ

- 全部を覚えようとせず、一周することを優先する
- 用語は自分の言葉で言い換えたメモを作りながら読む

---

## Phase 2：論理設計（正規化・ER図）

> 進め方・環境は未定。決まり次第追記する。

### 教材候補

- **『達人に学ぶDB設計徹底指南書 第2版』**（ミック著、翔泳社、2024年）
  - 論理設計と物理設計、正規化、ER図、非正規化とパフォーマンス、アンチパターン、グレーノウハウ、木構造の扱いまで網羅
  - 章ごとに練習問題あり

### メモ

- 正規化は**第3正規形まで**を優先して理解する。第4・第5正規形は「存在を知っている」程度でよい
- 「関数従属」「推移的」などの用語は自分の言葉で言い換える（例：「Aが決まればBが決まる関係」）

---

## Phase 3：要件定義〜運用までの全体像（任意）

> 進め方・環境は未定。決まり次第追記する。

### 教材候補

- **『やさしいデータベース設計 要件定義から運用までの勘どころ』**（衛藤豊著、インプレス、2026年1月）
  - 論理設計から物理設計、運用設計、基盤アーキテクチャ設計まで工程全体を扱う
  - 巻末に練習問題あり

### メモ

- Phase 2の後に余裕があれば取り組む
- インフラ寄りのキャリアを目指すなら、運用設計の章が特に参考になる

---

## Phase 4：実践・アンチパターン

> 進め方・環境は未定。決まり次第追記する。

### 教材候補

- **『SQLアンチパターン 第2版』**（Bill Karwin著、オライリー・ジャパン、2025年）
  - 書名に反して設計寄りの内容。論理設計・物理設計・インデックス設計・セキュリティまで扱う

### メモ

- 頭から通読せず、設計で迷ったときに該当章を読む辞書的な使い方をする

---

## 並行：Card Pulseで設計を実践

> 進め方は未定。決まり次第追記する。

### 考えられる設計テーマ

- 「カード」「収録セット」「価格の情報源」「価格の観測記録（時系列）」をどうテーブルに分けるか
- 同じカードが複数サイトで別名になっている場合にどう紐付けるか
- 価格履歴は上書きするか、追記するか

### メモ

- Phase 2の本を1章読むごとに、Card PulseのER図を描き直してみる
- Phase 1で作ったPostgreSQL環境をそのまま設計の検証に使える
